{
  "installCommand": "bun install --frozen-lockfile",
  "buildCommand": "bun prisma generate && DATABASE_URL=${DIRECT_DATABASE_URL:-$DATABASE_URL} bun run db:migrate:deploy && DATABASE_URL=${DIRECT_DATABASE_URL:-$DATABASE_URL} bun run db:seed && bun run build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/process-sync-jobs",
      "schedule": "0 3 * * *"
    }
  ]
}

